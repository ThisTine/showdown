services:
  showdown:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      STATIC_DIR: /app/dist
    restart: unless-stopped
